const router = require('express').Router();
const { Comment } = require('../../models');
const withAuth = require('../../utils/auth');

// Post -- (withAuth)

// Delete by id  -- (withAuth)

module.exports = router;